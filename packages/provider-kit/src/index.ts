/**
 * Provider kit owns adapter contracts, entity binding, and reusable shape helpers.
 * Callers can build adapters against this surface without depending on schema construction or query execution internals.
 */
export * from "./provider";
export * from "./provider/shapes";
