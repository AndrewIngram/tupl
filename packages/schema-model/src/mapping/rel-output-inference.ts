/**
 * Rel-output inference is the curated internal export surface for inferred output definitions and row mapping.
 */
export { inferAndMapRelOutputRows } from "./rel-output-mapping";
export { buildRelOutputCoercion, inferRelOutputDefinitions } from "./output-inference";
