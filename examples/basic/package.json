{
  "name": "@sqlql/example-basic",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@sqlql/core": "workspace:*",
    "@sqlql/executor-memory": "workspace:*",
    "@sqlql/sql": "workspace:*"
  }
}
