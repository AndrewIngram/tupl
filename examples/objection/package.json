{
  "name": "@sqlql/example-objection",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@sqlql/example-shared": "workspace:*",
    "@sqlql/objection": "workspace:*",
    "better-sqlite3": "^12.6.2",
    "knex": "^3.1.0",
    "objection": "^3.1.5",
    "sqlql": "workspace:*"
  }
}
